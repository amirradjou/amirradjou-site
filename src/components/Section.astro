---
interface Props {
  id: string;
  title: string;
}

const { id, title } = Astro.props;
---

<section class="section" aria-labelledby={`${id}-heading`}>
  <h2 id={`${id}-heading`}>{title}</h2>
  <slot />
</section>
